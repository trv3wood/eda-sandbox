services:
  eda-agent:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
      target: agent
    image: localhost/eda-agent:local
    working_dir: /workspace
    volumes:
      - .:/workspace

  eda-uhdm:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
      target: eda-uhdm
    image: localhost/eda-uhdm:local
    working_dir: /workspace
    volumes:
      - .:/workspace

  eda-rtl:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
      target: rtl-tools
    image: localhost/eda-rtl:local
    working_dir: /workspace
    volumes:
      - .:/workspace

  eda-scc:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
      target: eda-scc
    image: localhost/eda-scc:local
    working_dir: /workspace
    volumes:
      - .:/workspace
