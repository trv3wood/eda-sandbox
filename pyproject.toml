[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "systemc-tlm-agent"
version = "0.1.0"
description = "Evidence-grounded SystemC/TLM modeling workflow"
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.20",
  "openpyxl>=3.1",
  "pydantic>=2.6",
  "python-docx>=1.1",
  "PyYAML>=6.0",
]

[project.scripts]
systemc-tlm-agent = "systemc_tlm_agent.cli:main"
systemc-tlm-package = "systemc_tlm_model_packager.cli:main"
eda-uhdm = "eda_query.uhdm_runtime:main"
eda-uhdm-produce = "systemc_tlm_agent.tool_producers:uhdm_main"
eda-spec-produce = "systemc_tlm_agent.graph_extract:spec_main"

[project.optional-dependencies]
graph = [
  "duckdb>=1.0",
  "networkx>=3.2",
  "openai>=1.40",
]
vector = [
  "duckdb>=1.0",
  "faiss-cpu>=1.8",
  "sentence-transformers>=3.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
